palavra_secreta = "banana"
tamanho_palavra_secreta = len(palavra_secreta)
lista = []
for palavra_secreta in range(tamanho_palavra_secreta):
    lista.insert(palavra_secreta, "_")
carr = lista
print(carr)

